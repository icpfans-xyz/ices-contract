service : {
     "login": (event_key : text) -> (bool);
     "register": () -> (bool);
     "transfer": (principal, principal, nat) -> (bool);
     "set_ices_canister": (canister_id : text) -> ();
     "get_ices_canister": () -> (text);
}